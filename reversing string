#include <stdio.h>
int reverse(char *str){
    if(*str=='\0'){
        return 1;
    }
    else{
        reverse(str+1);
        putchar(*str);
    }
}
int main() {
    char str[100];
    printf("enter string: ");
    scanf("%s",&str);
    reverse(str);

    return 0;
}
