#include <stdio.h>
void reverse(const char *str){
    if(*str=='\0'){
        return;
    }
    else{
        reverse(str+1);
        putchar(*str);
    }
}
int main() {
   char str[]="hello world";
   reverse(str);
    return 0;
}
