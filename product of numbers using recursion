#include<stdio.h>
int product(int x){
    if(x==1){
        return 1;
    }
    return x* product(x-1);
}
int main(){
    int x,result;
    printf("enter number: ");
    scanf("%d",&x);
    result= product(x);
    printf("product is %d",result);
    return 0;
}
