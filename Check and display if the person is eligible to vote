int main() {
    int age;
    printf("enter your age: ");
    scanf("%d",&age);
    if(age=<18){
        printf("person cannot vote");
    }
    else{
        printf("person can vote");
    }
    return 0;
}
